db:
    image: mysql
    environment:
        - MYSQL_ROOT_PASSWORD=root
        - MYSQL_DATABASE=choropleth
web:
    build: .
    command: python src/manage.py runserver 0.0.0.0:8000
    volumes:
        - .:/app/
    ports:
        - "8000:8000"
    links:
        - db
